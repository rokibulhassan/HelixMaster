<div class="row">
  <div class="col-lg-4">
    <%= form_for(@city) do |f| %>
        <% if @city.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h2>

              <ul>
                <% @city.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
        <% end %>

        <div class="form-group">
          <%= f.label :name %>
          <br/>
          <%= f.text_field :name, :class => "form-control" %>
        </div>
        <!--<div class="form-group">-->
          <!--<%#= f.label :country_id %>-->
          <!--<br/>-->
          <!--<%#= select :city, :country_id, Country.all.collect { |c| [c.name, c.id] }, {:prompt => 'Select Country'}, {:class => 'form-control'} %>-->
        <!--</div>-->
        <div class="form-group">
          <%= f.label :state_id %>
          <br/>
          <%= select :city, :state_id, State.all.collect { |c| [c.name, c.id] }, {:prompt => 'Select State'}, {:class => 'form-control'} %>
        </div>
        <div class="form-actions">
          <%= f.submit %>
        </div>
    <% end %>
  </div>
</div>
