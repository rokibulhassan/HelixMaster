<div class="row">
  <div class="col-lg-4">
    <%= form_for(@location_type) do |f| %>
        <% if @location_type.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@location_type.errors.count, "error") %> prohibited this location_type from being
                saved:</h2>

              <ul>
                <% @location_type.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
        <% end %>

        <div class="form-group">
          <%= f.label :name %>
          <br/>
          <%= f.text_field :name, :class => "form-control" %>
        </div>
        <div class="form-actions">
          <%= f.submit %>
        </div>
    <% end %>
  </div>
</div>
