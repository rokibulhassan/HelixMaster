<% @page_heading = @record.class.name %>

<p><%= "Are you sure you want to delete this "+@record.class.name.downcase%></p>
<%= form_tag :action => "destroy", :id => @record.id do %>
<div class='form-actions'><%= link_to 'Cancel', :action => 'index'%><input type="submit" value='delete' class="btn btn-danger" /></div>
<% end %>
